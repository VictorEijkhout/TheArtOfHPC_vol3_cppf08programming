// /vectoridxmax.cpp
  int tmp_idx = 0;
  int tmp_max = numbers.at(tmp_idx);
  for (int i=0; i<numbers.size(); ++i) {
    int v = numbers.at(i);
    if (v>tmp_max) {
      tmp_max = v; tmp_idx = i;
    }
  }
  cout << "Max: " << tmp_max
       << " at index: " << tmp_idx << '\n';
