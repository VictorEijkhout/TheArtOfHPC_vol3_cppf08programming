for (int i=0; i<10; i++) {
  bool found{false};
  for (int j=0; j<10; j++) {
    if (i*j>threshold) {
      cout << "The pair " << i << "," << j
           << " multiplies to " << i*j << '\n';
      found = true;
      break;
    }
  }
  if (found) break;
}
