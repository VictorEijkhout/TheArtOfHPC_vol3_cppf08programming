// /passvaluelocal.cpp
double squared( double x ) {
  x = x*x;
  return x;
}
    /* ... */
// /passvaluelocal.cpp
  number = 5.1;
  cout << "Input starts as: "
       << number << '\n';
  other = squared(number);
  cout << "Output var is: "
       << other << '\n';
  cout << "Input var is now: "
       << number << '\n';
